{% include 'default/_form.html.twig' with {
    page_title: 'title.tag_edit',
    page_title_param: {'%id%': tag.id},
    attr: {
        method: 'post',
        action: url('tags_edit', {'id': tag.id}),
    },
    back_to_list_path: 'tags_index',
} %}